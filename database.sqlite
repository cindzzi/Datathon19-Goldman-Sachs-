select * into newtable from "Tacos"
where "Menu" = 'Burritos'

select * into newTableTaco from "Tacos"
where "Menu" = 'Tacos'

Select "Zip Code" from newtable
where "Zip Code" = 'Mexican Restaurant'

select "Categories"
from "newtable"
group by "Categories";

